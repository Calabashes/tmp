zi yu test 
CPU大小端导致整形或者short int型的字节存放顺序不一样
INTEL是小端模式，ARM是大端模式
0X12345678，在小端模式下面，由低地址到高地址存放为 0X78， 0X56， 0X34， 0X12
	    在大端模式下面，由低地址到高地址存放顺序为 0X12， 0X34， 0X56， 0X78
1. 在test.c中，S1， S2， S3确定由低地址存放到高地址，与CPU的类型无关
所以S1=4， S2 = 4， S3 = 1
2.网上说位域不能跨自己存放，是不正确的，而应该是不能跨单元存放，比如short类型的单元是2个字节的，就可以连续存放大于8个位的数。
